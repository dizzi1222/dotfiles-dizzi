
# ${ MODIFICADO POR DIEGO} FIN
# source = ~/.config/hypr/myColors.conf
source = ~/.config/hypr/keybinds.conf
# nueva config + Widgets de zenitie GOD

# ${ MODIFICADO POR DIEGO} FIN
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/

# ${ MODIFICADO POR DIEGO} FIN
monitor=,preferred,auto,1
# PARA DESACTIVAR LAS UPDATES:
# ecosystem:no_update_news = true # yo no lo activaria,rpero pa.

# ${ MODIFICADO POR DIEGO} FIN

# monitor=,highres,auto,auto
# monitor = eDP-1,1366x768,0x0,1
#config de classmate
# monitor=HDMI-A-2,1920x1080,auto,1,mirror # Añadido del archivo Zeni

# unscale XWayland
xwayland {
  force_zero_scaling = false
}


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
# ${ MODIFICADO POR DIEGO} FIN
# $screenshot = grim -g "$(slurp)" - | wl-copy # esto es lo que usa gil, pero yo uso gslurp con gyazo y rofi
$terminal = ghostty #o pones kitty god
# FIN ${ MODIFICADO POR DIEGO} FIN

$menu = rofi -show combi
# $clipboard = cliphist list | rofi -dmenu | cliphist decode | wl-copy
$clipboard = cliphist list | wofi --dmenu --config ~/.config/wofi/config_clipboard | cliphist decode | wl-copy
$color_picker = hyprpicker -a -n
$lock = hyprlock
# ${ MODIFICADO POR DIEGO}
# Dark wallpaper
$dark_wal = ls ~/wallpapers/wallpapers/ | rofi -dmenu | xargs -I _ sh -c 'swww img -t wipe ~/wallpapers/wallpapers/_ && wal -i ~/wallpapers/wallpapers/_'

# Light wallpaper
$light_wal = ls ~/wallpapers/wallpapers/ | rofi -dmenu | xargs -I _ sh -c 'swww img -t wipe ~/wallpapers/wallpapers/_ && wal -l -i ~/wallpapers/wallpapers/_'

$browser = google-chrome-stable
# FIN ${ MODIFICADO POR DIEGO} FIN


#################
### AUTOSTART ###parece que ya esta bien! y en win
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal

exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = nm-applet & volctl & greenclip daemon & nm-applet & blueman-applet
exec-once = dunst -config ~/.config/dunstrc
exec-once = wl-paste --watch cliphist store
exec-once = sleep 4 && waybar
exec-once = swww-daemon  #entra en conflicto con hypraper 🚨SI SOPORTA GIF!
# Configuración para el zoom literal, como el de los gestos.
# Requiere que hyprzoom esté activado en tu compilación.
exec-once = hyprctl setprop float:hyprzoom:zoom 1

# ${ MODIFICADO POR DIEGO} 
exec-once = sh ~/.config/hypr/scripts/text_animation/anitext.sh
exec-once = sh ~/.config/hypr/scripts/weather/weather.sh

exec-once = wl-paste --type image --watch cliphist store
exec-once = copyq
# Iniciar gnome-keyring  {error molesto}
# [key paring fix]
# Iniciar gnome-keyring
exec-once = /usr/bin/gnome-keyring-daemon --daemonize --replace --components=pkcs11,secrets,ssh,gpg
# exec-once = ~/.config/hypr/scripts/unlock_keyring.sh # Este script lo crearemos en el siguiente paso

# icono de updaaes [ no funciona en hyprland?]
exec-once = pamac-tray

# otra forma de arrancr drive, stow gOD 
exec-once = /home/diego/montar_gdrive.sh

# Añadidos del archivo Zeni 🚨NO SOPORTA GIF...
# exec-once = hyprpaper #entra en conflicto con swww ..


# ⚙️👉 3- Montar/desmontar el Widget personal
# https://elkowar.github.io/eww/#running-eww

# exec-once = [workspace 3] gnome-system-monitor # o kitty # arrancar desde el workpace 3 {oopcional} #con kitty fastfetch
# exec-once = [workspace 2] gnome-disks # o kitty # arrancar desde el workpace 3 {oopcional} #con kitty fastfetch
# exec-once = [workspace 1] sleep 7 && kitty # o ghostty
#
# exec-once = sleep 1 && eww open bar # prefiero la side-bar
exec-once = sleep 1 && eww open side-bar

#otras configs de zenities # ${ MODIFICADO POR DIEGO}
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = hypridle

#otras configs de zenities # FIN ${ MODIFICADO POR DIEGO} FIN

#############################
### ENVIRONMENT VARIABL`ES ###
#############################

# ${ MODIFICADO POR DIEGO}
#

# Iniciar gnome-keyring  {error molesto}
# [key paring fix env]
# Configurar el entorno para que las aplicaciones sepan que estamos en un entorno Wayland con un llavero
env = XDG_CURRENT_DESKTOP,GNOME
env = GNOME_SHELL_SESSION_MODE,classic
# Puedes probar también con "Hyprland" o "wayland" si "GNOME" causa problemas inesperados
# env = XDG_CURRENT_DESKTOP,Hyprland

# Forzar a Firefox a usar wayland, problemas con pantalla
env = MOZ_ENABLE_WAYLAND,1
# Para Hyprland: env = XDG_DATA_DIRS,/var/lib/flatpak/exports/share:/home/diego/.local/share/flatpak/exports/share:/usr/local/share:/usr/share

# Para aplicaciones Qt5
env = QT_QPA_PLATFORMTHEME,qt5ct

# Para aplicaciones Qt6
env = QT_QPA_PLATFORMTHEME,qt6ct

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = HYPRLAND_TRACE=1
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = NIXOS_OZONE,1
env = GDK_BACKEND,wayland,x11,*
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_QPA_PLATFORM,wayland;xcb
# env = GDK_SCALE,2.5
# env = GDK_DPI_SCALE,0.5
# env = _JAVA_OPTIONS,"-Dsun.java2d.uiScale=2"

# Variables de entorno adicionales de Zeni (sin duplicar las existentes)
env = XCURSOR_THEME,MokouX # Se mantiene el XCURSOR_SIZE de dizzi
env = HYPRCURSOR_THEME,NotwaitaBlack


#####################
### LOOK AND FEEL ###
#####################

source = ~/.cache/wal/colors-hyprland.conf


# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 0
    gaps_out = 0

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = $color1 $color2 45deg
    col.inactive_border = $background

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}


#otras configs de zenities # ${ MODIFICADO POR DIEGO}
# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 14

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.95

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
        vibrancy = 0.8
        contrast = 0.6
        size = 8
        passes = 2
        ignore_opacity = false
    }

    shadow{
        enabled = false
        sharp = false
    }
    #blurls = gtk-layer-shell
    
}
#otras configs de zenities # FIN ${ MODIFICADO POR DIEGO} FIN

# https://wiki.hyprland.org/Configuring/Variables/#animations
#
# animations {
#     enabled = true
#
#     # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#
#     bezier = myBezier, 0.05, 0.9, 0.1, 1.05
#
#     animation = windows, 1, 7, myBezier
#     animation = windowsOut, 1, 7, default, popin 80%
#     animation = border, 1, 10, default
#     animation = borderangle, 1, 8, default
#     animation = fade, 1, 7, default
#     animation = workspaces, 1, 6, default
# }
# ANIMACIONES DE GIL ARRIBA. > > Left Right animations

#otras configs de zenities # ${ MODIFICADO POR DIEGO}
# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.8, 0.1, 1

    animation = windows, 1, 6, myBezier
    animation = windowsOut, 1, 7, default, popin 90%
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default, slidevert
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    # force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers


#  ${ MODIFICADO POR DIEGO} 

    force_default_wallpaper = -0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(

# FIN ${ MODIFICADO POR DIEGO} FIN

		new_window_takes_over_fullscreen = true


		exit_window_retains_fullscreen = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_options = fkeys:basic_13-24
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

       # cambiar mouse boton
        left_handed = true

    follow_mouse = 1

    sensitivity = 0.6 # -1.0 - 1.0, 0 means no modification.

    # Para touchpad mouse: activar desplazamiento invertido
    touchpad {
        natural_scroll = false
    }
    # Para mouse normal: activar desplazamiento invertido
    natural_scroll = false       # agrega esta línea fuera de touchpad {}
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

# Añadidos del archivo Zeni
device {
    name = pixart-usb-optical-mouse
    # cambiar mouse boton de dispositivo
    left_handed = true
}

device {
    name = compx-2.4g-wireless-receiver-mouse
    # cambiar mouse boton de dispositivo
    left_handed = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
# device {
#     name = epic-mouse-v1
#     sensitivity = -0.5
#     left_handed = true
#
# }

####################
### KEYBINDINGSS ###
####################

binds {


		hide_special_on_workspace_change = true


		movefocus_cycles_fullscreen = true


		movefocus_cycles_groupfirst = true


}

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# ${ MODIFICADO POR DIEGO} 

# Habilitar Alt + Tab para ciclar entre ventanas
# {opcional}
# exec-once = hyprswitch init & # Iniciar el demonio de hyprswitch al inicio
# bind = ALT, tab, exec, hyprswitch # Usar hyprswitch para el atajo Alt+Tab

# lista visual de ventanas recientes, y soltar Alt para seleccionar.
# {opcional}
#
# Ten en cuenta que esto ciclará entre ventanas en el espacio de trabajo actual por defecto.
# Si quieres que Alt+Tab funcione globalmente entre todos los espacios de trabajo,
# tendrías que usar 'cyclenext' con el parámetro 'all'.
bind = SHIFT, tab, focuscurrentorlast # Cicla entre ventanas en el espacio de trabajo actual
# Cicla entre ventanas en el espacio de trabajo actual
bind = ALT, tab, cyclenext, prev all # Cicla en orden inverso
# Si quieres que Alt+Tab funcione entre *todos* los espacios de trabajo:
# bind = ALT, tab, cyclenext, all
# bind = ALT SHIFT, tab, cyclenext, prev all

# ${ MODIFICADO POR DIEGO}


# Abrir COPYQ - Visualizar imagenes , cerrar con ESC o ALT TAB
bind = SUPER SHIFT, V, exec, copyq toggle  # anteriormente: easyeffects o podria ser pavucontrol
# Cliphist de Gil
bind = SUPER, V, exec, $clipboard
# bind = SUPER, V, exec, ~/.config/hypr/scripts/clipboard_history.sh #o bien podria usar /home/diego/.config/hypr/scripts/clipboard_history-rofi-cliphist.sh
bind = Control+Alt, K, exec, kitty
bind = Control+Alt, T, exec, ghostty
# Abrir explorador de archivos
bind = SUPER, E, exec, nautilus 
# Abrir navegador
bind = SUPER, BACKSPACE, exec, ghostty -e kew # scrcpy # podria usarlo, abre el emulador movil
# para cambiar el path de musica en [kew] {utiliza};
# kew path "/home/joe/Musik/" (changes the path
# Abrir navegador
bind = , F1, exec, google-chrome-stable # firefox
# bind = SHIFT, F1, exec, google-chrome-stable 
# Abrir Yazi ver imagenes, archivos, similar a fzf
bind = SUPER, M, exec, kitty --class YaziTerminal -- yazi
# abrir fzf con nvim es un poco lento..
# Abre Neovim con selección de archivo FZF usando un script
bind = SUPER SHIFT, M, exec, ~/.config/hypr/scripts/open_nvim_fzf.sh
# Abre un panel de emojis

# period = .
bind = SUPER, period, exec, rofimoji --action copy
# Abrir lollypop MP3 - reproductor
# bind = , F4, exec, youtube-music

# Cerrar la ventana activa con Alt + F4
bind = ALT, F4, killactive,

# Mostrar Menu - Atajo para Gyazo - Captura Fullscren con la tecla Print Screen
# Fullscreen abre gyazo, clip area no ya que usa hyprshot
bind = , Print, exec,~/.local/bin/gyazo-wayland-captura-menu-rofi
# pueden entrar en conflictos con: keybinds.conf usa hyprshot, mi scripts es una mezcla de ambos.

# Atajo para gyazo este clip area si lo abre. SUPER + Control + C → Seleccionar área directamente - con slurp grim 
bind = SUPER CONTROL, C, exec, ~/.local/bin/gyazo-wayland-captura-clip


# En tu hyprland.conf

# Abrir btop con Ctrl + Shift + Esc - Monitor del sistema
bind = CONTROL SHIFT, ESCAPE, exec, gnome-system-monitor # puede ser btop, o htop
# luego recarga la configuración de Hyprland (normalmente con hyprctl reload 


# Simular Alt + F4, Alt+F3: hyprctl, poweroff, reboot
#
# 1. Necesitas instalar:
# sudo pacman -S jq
# jq = procesar datos JSON
#
# 2. Apagar el sistema si no hay ventanas (con Super + Alt + F4)

bind = SUPER ALT, F4, exec, systemctl poweroff

# Reiniciar el sistema directamente con Super + Alt + F3
bind = ALT, F3, exec, systemctl reboot

# 3. Dar Permisos de Ejecución a los Scripts en la terminal
# chmod +x ~/.config/hypr/scripts/smart_poweroff.sh

# Borrar sesión con Windows (Super) + L
bind = SUPER SHIFT, ESCAPE, exec, hyprctl dispatch exit
# Cerrar sesión con Windows (Super) + L
bind = SUPER, ESCAPE, exec, $lock
# FIN ${ MODIFICADO POR DIEGO} FIN
# ... (el resto de tus binds) ...



# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = SUPER, RETURN, exec, $terminal
bind = SUPER, Q, killactive,
#  ${ MODIFICADO POR DIEGO} FIN
#  Atajos de firefox - con WTyPe ; sudo pacman -S wtype
#  abrir barra direccion


#💫 FORZAR CAMBIOS DE TECLA 💫
# bind = CTRL, E, exec, wtype -M ctrl l -m ctrl
# bindl = MODIFIER, KEY, exec, COMMAND
# bindl = CONTrOL, W, exec, wtype -M CONTROL BACKSPACE 
# Presiona Ctrl y Backspace
# bindl = CTRL, W, exec, wtype -M ctrl BackSpace


# {NO FUNCIONA..}tabear con numeros firefox - con WTyPe ; sudo pacman -S wtype
#

# Controlar como cierra la shell/terminal
bind = SUPER CONTROL, Q, exec, hyprctl dispatch exit # ¡CAMBIADO AQUÍ!
# O lo cambias a preguntar antes de cerrar
# bind = $mainMod CONTROL, Q, exit,
# FIN ${ MODIFICADO POR DIEGO} FIN

bind = $mainMod, F, togglefloating,
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod SHIFT, C, exec, $color_picker
# Abre el menú de selección de fondos de pantalla

# ${ MODIFICADO POR DIEGO}
# ${ MODIFICADO POR DIEGO}
bind = $mainMod, B, exec, zsh -c "~/.local/bin/walset-menu"

# bind = $mainMod, B, exec, $dark_wal
bind = $mainMod SHIFT, B, exec, $light_wal
# bind = $mainMod CONTROL SHIFT, L, exec, $lock #config de gil

# en conflicto con lanzar eww bar # en conflicto con lanzar eww bar
# bind = SUPER SHIFT, R, exec, hyprctl reload

# 📌🚨 [Mejor usar el Volumen de Zenities {EWW}] !!!!!! Subir/bajar volumen con scroll sobre cualquier ventana !!!!!!  📌🚨 

# Scroll up → sube volumen +5%#
# bind = , mouse_down, exec, sh -c 'pactl set-sink-volume @DEFAULT_SINK@ +5%'

# Scroll down → baja volumen -5%
# bind = , mouse_up, exec, sh -c 'pactl set-sink-volume @DEFAULT_SINK@ -5%'

# FIN ${ MODIFICADO POR DIEGO} FIN
bind = $mainMod, TAB, exec, hyprctl dispatch fullscreen 1
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume 0 -5%
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume 0 +5%
bind = , XF86KbdLightOnOff, exec, sudo kbd-backlight-toggle
bind = , XF86MonBrightnessDown, exec, brightnessctl set 10-%
bind = , XF86MonBrightnessUp, exec, brightnessctl set +10%
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
bind = $mainMod, F11, fullscreen

# Move focus with mainMod + arrow keys
# bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move window
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# resize window
bind = $mainMod CONTROL, h, resizeactive, -40 0
bind = $mainMod CONTROL, l, resizeactive, 40 0
bind = $mainMod CONTROL, k, resizeactive, 0 -40
bind = $mainMod CONTROL, j, resizeactive, 0 40

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# cerrar terminal divida, kitty,ghostty:
# Control + ;  o Control + ñ

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1

#  ${ MODIFICADO POR DIEGO} 
# windowrulev2 = size 800 600, class:^(ghostty)$
# FIN ${ MODIFICADO POR DIEGO} FIN

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Reglas específicas para VLC
windowrulev2 = float, class:^(vlc)$
windowrulev2 = size 490 390, class:^(vlc)$
windowrulev2 = move 50 50, class:^(vlc)$
windowrulev2 = noinitialfocus, class:^(vlc)$

# REGLAS PARA ABRIR VLC, Loupe etc mp4 jpg imagenes, videos
# windowrulev2 = float, class:^(vlc|mpv|feh|sxiv|org.gnome.Loupe)$

windowrulev2 = noinitialfocus, class:^(vlc|mpv|feh|sxiv|org.gnome.Loupe)$

# REGLAS para CHROME
windowrule = float, class:^(google-chrome)$ initialTitle:.*Google Chrome.*
windowrule = size 664 727, class:^(google-chrome)$ initialTitle:.*Google Chrome.*
windowrule = move 35 40, class:^(google-chrome)$ initialTitle:.*Google Chrome.*

# REGLAS para Firefox 
windowrule = float, class:^(firefox)$ initialTitle:.*Mozilla Firefox.*
windowrule = size 664 727, class:^(firefox)$ initialTitle:.*Mozilla Firefox.*
windowrule = move 35 40, class:^(firefox)$ initialTitle:.*Mozilla Firefox.*

# REGLAS PARA VENTANAS DE LOGIN DE GOOGLE EN TODOS LOS NAVEGADORES
# Chromium
windowrule = float, class:^(chromium)$ initialTitle:.*Chromium.*
windowrule = size 664 727, class:^(chromium)$ initialTitle:.*Chromium.*
windowrule = move 35 40, class:^(chromium)$ initialTitle:.*Chromium.*

# Brave Browser
windowrule = float, class:^(brave-browser)$ initialTitle:.*Brave.*
windowrule = size 664 727, class:^(brave-browser)$ initialTitle:.*Brave.*
windowrule = move 35 40, class:^(brave-browser)$ initialTitle:.*Brave.*

# Microsoft Edge (sí disponible en Linux)
windowrule = float, class:^(microsoft-edge)$ initialTitle:.*Microsoft Edge.*
windowrule = size 664 727, class:^(microsoft-edge)$ initialTitle:.*Microsoft Edge.*
windowrule = move 35 40, class:^(microsoft-edge)$ initialTitle:.*Microsoft Edge.*

# Opera
windowrule = float, class:^(opera)$ initialTitle:.*Opera.*
windowrule = size 664 727, class:^(opera)$ initialTitle:.*Opera.*
windowrule = move 35 40, class:^(opera)$ initialTitle:.*Opera.*

# Vivaldi
windowrule = float, class:^(vivaldi-stable)$ initialTitle:.*Vivaldi.*
windowrule = size 664 727, class:^(vivaldi-stable)$ initialTitle:.*Vivaldi.*
windowrule = move 35 40, class:^(vivaldi-stable)$ initialTitle:.*Vivaldi.*

# Navegadores basados en Electron/CEF (como Discord, Slack, etc.)
windowrule = float, class:^(electron|cef)$ title:.*Google Accounts.*
windowrule = size 664 727, class:^(electron|cef)$ title:.*Google Accounts.*
windowrule = move 35 40, class:^(electron|cef)$ title:.*Google Accounts.*

# REGLAS PARA ABRIR 
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
