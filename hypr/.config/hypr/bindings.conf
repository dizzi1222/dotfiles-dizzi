
####################
### KEYBINDINGSS ###
####################

# === TIPOS DE BIND EN HYPRLAND ===
# bind   = Ejecución ÚNICA al presionar la tecla
# binde  = Se REPITE mientras mantenés presionado (útil para resize/move)
# bindm  = Bind de MOUSE (para mover/redimensionar ventanas con el mouse)
# bindr  = Se ejecuta al SOLTAR la tecla (release)
# bindl  = Funciona incluso con pantalla BLOQUEADA (lock)
# bindn  = NO consume el bind, permite que otras apps lo capturen también
# bindrt = Repite al soltar (release + repeat)

# Ejemplos de uso:
# bind  = $mainMod, Q, killactive          # Cierra ventana (una vez)
# binde = $mainMod CTRL, h, resizeactive   # Resize continuo (mantener pulsado)
# bindm = $mainMod, mouse:272, movewindow  # Arrastra ventana con mouse
# bindr = CAPS, exec, notify-send "Caps"   # Notifica al soltar CAPS
# bindl = , XF86AudioPlay, exec, playerctl # Reproduce incluso si está bloqueado
# bindn = , Print, exec, screenshot        # Otras apps también pueden usar Print

# #######################################################################################
# Configs DIZZI
# #######################################################################################
binds {


		hide_special_on_workspace_change = true


		movefocus_cycles_fullscreen = true


		movefocus_cycles_groupfirst = true


}

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
# ~TODO LO NUEVO, NOTIFACIONES, CONTROL DEL SISTEMA [microfono, wifi, volumen, bluetooth, widgets etc.]
bind = $mainMod, N, exec, swaync-client -t -sw        # Abrir panel
bind = $mainMod CONTROL, N, exec, killall swaync && swaync & # Recargarconfig swaync
bind = $mainMod SHIFT, N, exec, swaync-client -rs  # Recargar estilos swaync
# bind = $mainMod ALT, N, exec, notify-send "Notificaciones Pendientes" "$(swaync-client -c -sw) notificaciones"
bind = $mainMod ALT, N, exec, cliphist wipe # Limpiar historial de CLIPBOARD
bind = SUPER, Z, exec, sh ~/scripts/system_control.sh  # O usar scrcpy; abre el emulador movil
# Bloquear ventana activa con el mouseu
bind = $mainMod SHIFT, L, exec, hyprctl dispatch lockactivegroup lock
# ${ MODIFICADO POR DIEGO}

# Habilitar Alt + Tab para ciclar entre ventanas
# {opcional}
# exec-once = hyprswitch init & # Iniciar el demonio de hyprswitch al inicio
# bind = ALT, tab, exec, hyprswitch # Usar hyprswitch para el atajo Alt+Tab

# lista visual de ventanas recientes, y soltar Alt para seleccionar.
# {opcional}
#
# Ten en cuenta que esto ciclará entre ventanas en el espacio de trabajo actual por defecto.
# Si quieres que Alt+Tab funcione globalmente entre todos los espacios de trabajo,
# tendrías que usar 'cyclenext' con el parámetro 'all'.
bind = ALT, tab, focuscurrentorlast # Cicla entre ventanas en el espacio de trabajo actual
# Cicla entre ventanas en el espacio de trabajo actual

# bind = SHIFT, tab, cyclenext, all # Cicla en orden inverso
# COMENTE SHIFT POR:
bind = SUPER, BACKSPACE, cyclenext, all # Cicla en orden inverso
bind = ALT, BACKSPACE, cyclenext, all # Cicla en orden inverso
# bind = SHIFT, tab, cyclenext, prev all # Cicla en orden inverso
# Si quieres que Alt+Tab funcione entre *todos* los espacios de trabajo:

# ${ MODIFICADO POR DIEGO}

		# | ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
		# ╰─❯ 📌	   CAMBIAR FONDO CAELESTIA, REMPLAZA ZENITIES [sww, hyprpaper, quickshell]..
			# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

#📌 🚨 ~ Para un resumen de mapeos+funciones mira:
# nvim /home/diego/mi_gdrive/-\ Linux\ Hyprland\ \[Arch\]/-\ \[Music\ Presence\]+Eww\ Zenities+Quickshell/montar\ Quickshell+Configs.md

#  🚨|~${FORMAS DE ACTIVAR waybar, eww }$~| 🚨
# [1] 󰖳 ~{reiniciar} killall eww:󰖳
bind = $mainMod, K, exec, killall eww
# [2] 󰖳 ~{reiniciar} killall waybar󰖳
bind = SUPER SHIFT, W, exec, killall waybar && /usr/bin/waybar &
bind = SHIFT ALT, W, exec, killall waybar
bind = CONTROL SHIFT, W, exec, /usr/bin/waybar &
# si quieres invocar waybar simplemente cambia de fondo con sww o hyprpaper

#  🚨|~${FORMAS DE CAMBIAR EL FONDO DE PANTALLA }$~| 🚨
# 1 󰖳 Cambiar fondo de SWW 󰖳 | 🚨SI SOPORTA GIF!
# bind = $mainMod, B, exec, zsh -c "$scp/walset-menu"
# 2 󰖳 Cambiar fondo de Zenities [Hyprpaper] 󰖳
# bind = $mainMod SHIFT, B, exec, sh ~/scripts/select_wallpaper.sh
# 3 󰖳 Cambiar fondo de Caelestia [Otra wea] 󰖳
# fondo de caelestia [Windows + L + >]

# |~${Para cambiar de fondo de caelestia usa [Windows + L + >] y: escribe:}$~|
#                                                             >wallpaper [enter], selecciona la imagen
#                                                         {y escribe el nombre de de la imagen despues de wallpaper}
#                                                         {ejemplo: wallpaper soygoku3.jpg [enter]}
#
#                                                          |~${}  y para un fondo aleatorio usa:}$~|
                                                    bind = $mainMod ALT, R, exec, caelestia wallpaper --random ~/wallpapers

#  🚨|~${} FORMAS DE ACTIVAR: Caelestia, SWW GIFS [dizzi], HYPRPAPER [zenities] }$~| 🚨
# [1] 󰖳 ~Activar caelestia wallpaper󰖳
bind = $mainMod, W, exec, ~/.config/hypr/scripts/launch_caelestia.sh
bind = SUPER SHIFT, Q, exec, quickshell &

# [1.5] 󰖳 ~Matar caelestia wallpaper {para luego iniciarlo}󰖳
bind = $mainMod, D, exec, swww-daemon
bind = $mainMod, D, exec, pkill -f hyprpaper
bind = $mainMod, D, exec, pkill -f caelestia
bind = $mainMod, D, exec, pkill -f quickshell
# [2] 󰖳 ~Activar hyprland wallpaper [zenities] # el logo de hyprlock 󰖳
# SUPER, A, activa hyprpaper # se sobrepone, quita el fondo de CAELESTIA, sww # asi lo tengo en hypr/keybinds.lua

# [3] 󰖳 ~Activar SWW GIFS sobre hyprpaper 󰖳
bind = SUPER SHIFT, A, exec, ~/.config/hypr/scripts/sww_reload.sh # activa sww # se sobrepone A TODO
bind = SUPER SHIFT, D, exec, ~/.config/hypr/scripts/sww_reload.sh # INICIAR SWW [Alt]
# 󱓈 󱓇 Es posible activar SWW sobre Caelestia!!...  

# 📌 Para establecer pywal sobre waybar y eww: USAS [1] 󰖳 Cambiar fondo de SWW, o GIFS 󰖳
# 📌 Para cambiar el wall/pfp icon: USAS [2] 󰖳 Cambiar fondo de Zenities [Hyprpaper] 󰖳

# 󰀦󰀦 🚨o7 Zenities, 🚨 o7 SWW GIF, hola CAELESTIA wallpaper!!🚨 󰀦󰀦
#
# Abrir COPYQ - Visualizar imagenes , cerrar con ESC o ALT TAB
bind = SUPER SHIFT, V, exec, copyq toggle  # anteriormente: easyeffects o podria ser pavucontrol
# Cliphist de Gil
bind = SUPER, V, exec, $clipboard
# bind = SUPER, V, exec, ~/.config/hypr/scripts/clipboard_history.sh #o bien podria usar /home/diego/.config/hypr/scripts/clipboard_history-rofi-cliphist.sh
bind = Control+Alt, K, exec, kitty
bind = Control+Alt, T, exec, ghostty
# Abrir explorador de archivos
bind = SUPER, E, exec, nautilus # Thunar
# Abrir navegador
# bind = SUPER, BACKSPACE, exec, $scp/toggle-music.sh # kitty -e kew
#                                                   | 
#                                                   ╰─❯ alternativa a: caelestia toggle music
# para cambiar el path de musica en [kew] {utiliza};
# kew path "/home/joe/Musik/" (changes the path
# Abrir navegador
bind = , F1, exec, brave # firefox # brave-browser # google-chrome-stable # opera # microsoft-edge # vivaldi-stable
bind = , F9, exec, firefox # brave-browser # google-chrome-stable # opera # microsoft-edge # vivaldi-stable
# Alternativa por si no me acuerdo xd:
bind = , F8, exec, firefox # brave-browser # google-chrome-stable # opera # microsoft-edge # vivaldi-stable
# bind = SHIFT, F1, exec, google-chrome-stable
# Abrir Yazi ver imagenes, archivos, similar a fzf
bind = SUPER ALT, M, exec, kitty --class YaziTerminal -- yazi
# abrir fzf con nvim es un poco lento..
# Abre Neovim con selección de archivo FZF usando un script
bind = SUPER CONTROL, M, exec, ~/.config/hypr/scripts/open_nvim_fzf.sh
# Abre un panel de emojis

# period = .
# bind = SUPER, period, exec, rofimoji --action copy # TA FEo
bind = SUPER SHIFT, space, exec, rofi -show combi
bind = SUPER CONTROL, space, exec, fuzzel --prompt="Looking for App:" # Alternativa a Rofi app launcher xd
bind = SUPER, period, exec, rofimoji --action copy --selector fuzzel  --prompt="Emoji Historial 󰁝 󰁅:"
bind = SUPER SHIFT, period, exec, rofimoji --action copy --selector fuzzel  --prompt="Nerd Font Historial 󰁝 󰁅:" --files nerd_font
# Abrir lollypop MP3 - reproductor
# bind = , F4, exec, youtube-music


# Mostrar Menu - Atajo para Gyazo - Captura Fullscren con la tecla Print Screen
# Fullscreen abre gyazo, clip area no ya que usa hyprshot
bind = , Print, exec, $scp/gyazo-wayland-captura-menu-rofi
# pueden entrar en conflictos con: keybinds.conf usa hyprshot, mi scripts es una mezcla de ambos.

# Atajo para gyazo este clip area si lo abre. SUPER + Control + C → Seleccionar área directamente - con slurp grim
bind = SUPER CONTROL, C, exec, $scp/gyazo-wayland-captura-clip

# [CAPTURA DE REGIONES Y VENTANAS ENFOCADAS]
# Screenshot
# Se eliminó --fullscreen para que Satty inicie como una ventana normal/flotante
# (Gracias a la windowrule anterior).

# 2. Captura de Región
# bindd = , PRINT, Satty Screenshot Region, exec, grim -g "$(slurp -o -r -c '##ff0000ff')" -t ppm - | satty --filename - --output-filename ~/Pictures/Screenshots/satty-$(date '+%Y%m%d-%H:%M:%S').png

# 3. Captura de Ventana Enfocada
bindd = SHIFT, PRINT, Satty Screenshot Window, exec, hyprctl activewindow -j | jq -r '"\(.at[0]),\(.at[1]) \(.size[0])x\(.size[1])"' | grim -g - -t ppm - | satty --filename - --output-filename ~/Pictures/Screenshots/satty-$(date '+%Y%m%d-%H:%M:%S').png

# 4. Captura de Pantalla Completa
bindd = SUPER, PRINT, Satty Screenshot Display, exec, grim -t ppm - | satty --filename - --output-filename ~/Pictures/Screenshots/satty-$(date '+%Y%m%d-%H:%M:%S').png

# Testing
bindl = Super, f12, exec, notify-send -u low -i dialog-information-symbolic 'Test notification' "Here's a really long message to test truncation and wrapping\nYou can middle click or flick this notification to dismiss it!" -a 'Shell' -A "Test1=I got it!" -A "Test2=Another action"

# En tu hyprland.conf

# Abrir btop con Ctrl + Shift + Esc - Monitor del sistema
bind = CONTROL SHIFT, ESCAPE, exec, gnome-system-monitor #o htop
bind = CONTROL, ESCAPE, exec, gnome-system-monitor #o REPEAT!
bind = SHIFT, ESCAPE, exec, gnome-system-monitor # REPEAT!
bind = SUPER CONTROL, ESCAPE, exec, kitty btop #o htop
# bind = CONTROL, TAB, exec, btop
# luego recarga la configuración de Hyprland (normalmente con hyprctl reload


# Simular Alt + F4, Alt+F3: hyprctl, poweroff, reboot
#
# 1. Necesitas instalar:
# sudo pacman -S jq
# jq = procesar datos JSON
#
# 2. Apagar el sistema si no hay ventanas (con Super + Alt + F4)

bind = SUPER ALT, F4, exec, systemctl poweroff

# Reiniciar el sistema directamente con Super + Alt + F3
bind = ALT, F3, exec, systemctl reboot

# 3. Dar Permisos de Ejecución a los Scripts en la terminal
# chmod +x ~/.config/hypr/scripts/smart_poweroff.sh

# Borrar sesión con Windows (Super) + L
bind = SUPER SHIFT, ESCAPE, exec, hyprctl dispatch exit
# Cerrar sesión con Windows (Super) + L
bind = SUPER+ALT, ESCAPE, exec, $lock
# FIN ${ MODIFICADO POR DIEGO} FIN
# ... (el resto de tus binds) ...



# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = SUPER, RETURN, exec, $terminal
# bind = SUPER, Q, killactive,
bind = SUPER, Q, killactive,
# Cerrar la ventana activa con Alt + F4
bind = ALT, F4, killactive,
# PARA CERRAR JUEGOS DE GEFORCe, en GAMESCOPE
bind = ALT, F4, exec, ~/scripts/kill-gamescope # Para cerrar Geforce inffinity puedes usar; Ctrl + Q
bind = ALT, Q, exec, ~/scripts/kill-gamescope # Para cerrar Geforce inffinity puedes usar; Ctrl + Q

#  ${ MODIFICADO POR DIEGO} FIN
#  Atajos de firefox - con WTyPe ; sudo pacman -S wtype
#  abrir barra direccion


#💫 FORZAR CAMBIOS DE TECLA 💫
# Activar busqueda chrome /// Contra: revierte autocompletar
bind = CTRL, E, exec, wtype -M ctrl l -m ctrl

# {NO FUNCIONA..}tabear con numeros firefox - con WTyPe ; sudo pacman -S wtype
#

# Controlar como cierra la shell/terminal
bind = SUPER CONTROL, Q, exec, hyprctl dispatch exit # ¡CAMBIADO AQUÍ!
# O lo cambias a preguntar antes de cerrar
# bind = $mainMod CONTROL, Q, exit,
# FIN ${ MODIFICADO POR DIEGO} FIN

# bind = $mainMod, F, togglefloating, # Antes de esto, en tu keybinds, agrega:
bind = $mainMod, F, exec, ~/.config/hypr/scripts/omarchy-hyprland-window-pop # Solo aplica para Picture-in-Picture, PIP, POP
# Toggle window pop (tamaño por defecto: 1300x900)

     # PARA UN FLOATING MAS AGRESIVO, En tu keybinds, agrega:
bind = $mainMod SHIFT, F, exec, hyprctl dispatch fullscreen 1

# O más simple:
bind = $mainMod SHIFT, F, togglefloating
bind = $mainMod SHIFT, F, centerwindow
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod SHIFT, C, exec, $color_picker
# Abre el menú de selección de fondos de pantalla

# ${ MODIFICADO POR DIEGO}

#  🚨|~${} FORMAS DE MODIFICAR EL FONDO DE PANTALLA }$~| 🚨
#  󰖳 Cambiar fondo de SWW. O GIFS 󰖳
bind = $mainMod, B, exec, zsh -c "$scp/walset-menu"
#  󰖳 Cambiar fondo de Caelestia [Otra wea] 󰖳
# fondo de caelestia [Windows + L + >]
#  󰖳 Cambiar fondo de Zenities [Hyprpaper] 󰖳
bind = $mainMod SHIFT, B, exec, sh ~/scripts/select_wallpaper.sh

# 🚨DESACTIVE EL WALLPAPER DE gil POR CONFLICTOS CON mis wallpaper!! 󰀦󰀦🚨
# bind = $mainMod, B, exec, $dark_wal
# bind = $mainMod SHIFT, B, exec, $light_wal

# bind = $mainMod CONTROL SHIFT, L, exec, $lock #config de gil

# en conflicto con lanzar eww bar # en conflicto con lanzar eww bar
# bind = SUPER SHIFT, R, exec, hyprctl reload

# 📌🚨 [Mejor usar el Volumen de Zenities {EWW}] !!!!!! Subir/bajar volumen con scroll sobre cualquier ventana !!!!!!  📌🚨

# Scroll up → sube volumen +5%#
# bind = , mouse_down, exec, sh -c 'pactl set-sink-volume @DEFAULT_SINK@ +5%'

# Scroll down → baja volumen -5%
# bind = , mouse_up, exec, sh -c 'pactl set-sink-volume @DEFAULT_SINK@ -5%'

# FIN ${ MODIFICADO POR DIEGO} FIN
# Ejecuta este comando para ver qué dispositivos LED están disponibles:
# ls -l /sys/class/leds/
bind = $mainMod, TAB, exec, hyprctl dispatch fullscreen 1
bind = $mainMod, T, exec, hyprctl dispatch fullscreen 1 # Alternar entre modo Tiling y fullscreen (Como GLAZEWM que usa Alt+T)

# Volumen de Sonido
# bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioMute, exec, ~/.config/hypr/scripts/volume-notification.sh mute
# bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume 0 -5%
binde = , XF86AudioLowerVolume, exec, ~/.config/hypr/scripts/volume-notification.sh down
# bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume 0 +5%
binde = , XF86AudioRaiseVolume, exec, ~/.config/hypr/scripts/volume-notification.sh up

# Iluminar el teclado con las teclas de arriba/abajo
bind = , XF86KbdLightOnOff, exec, sudo kbd-backlight-toggle
bind = , XF86KbdBrightnessUp, exec, ~/.config/hypr/scripts/kbd-backlight-notification.sh up
# bind = , XF86KbdBrightnessUp, exec, ~/scripts/kbd-backlight-up
bind = , XF86KbdBrightnessDown, exec, ~/.config/hypr/scripts/kbd-backlight-notification.sh down
# bind = , XF86KbdBrightnessDown, exec, ~/scripts/kbd-backlight-down

# Iluminacion
# bind = , XF86MonBrightnessDown, exec, brightnessctl set 10-%
bind = , XF86MonBrightnessDown, exec, ~/.config/hypr/scripts/brightness-notification.sh down
# bind = , XF86MonBrightnessUp, exec, brightnessctl set +10%
bind = , XF86MonBrightnessUp, exec, ~/.config/hypr/scripts/brightness-notification.sh up
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
bind = , F11, exec, hyprctl dispatch fullscreen 0
bind = $mainMod, F11, fullscreen

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Arrow keys for moving focus
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move window
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d
bind = $mainMod SHIFT, down, movewindow, d

# resize window
binde = $mainMod CONTROL, h, resizeactive, -40 0
binde = $mainMod CONTROL, left, resizeactive, -40 0
binde = $mainMod CONTROL, l, resizeactive, 40 0
binde = $mainMod CONTROL, right, resizeactive, 40 0
binde = $mainMod CONTROL, k, resizeactive, 0 -40
binde = $mainMod CONTROL, up, resizeactive, 0 -40
binde = $mainMod CONTROL, j, resizeactive, 0 40
binde = $mainMod CONTROL, down, resizeactive, 0 40

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


######################################3
### QUICKSHELL KEYBINDINGSS CAELESTIA 1##
######################################2#

# exec = hyprctl dispatch submap global
# submap = global

# ## Shell keybinds
# Launcher
#
# bind = $mainMod, A, global, #desactivar fondo caelestia
bind = $mainMod, L, global, caelestia:launcher
# bindin = Super, catchall, global, caelestia:launcherInterrupt
bindin = Super, mouse:272, global, caelestia:launcherInterrupt
bindin = Super, mouse:273, global, caelestia:launcherInterrupt
bindin = Super, mouse:274, global, caelestia:launcherInterrupt
bindin = Super, mouse:275, global, caelestia:launcherInterrupt
bindin = Super, mouse:276, global, caelestia:launcherInterrupt
bindin = Super, mouse:277, global, caelestia:launcherInterrupt
bindin = Super, mouse_up, global, caelestia:launcherInterrupt
bindin = Super, mouse_down, global, caelestia:launcherInterrupt

# Misc
# bind = $kbSession, global, caelestia:session
# LIMPIaR NOTIFACIONES - notis
bindl = SUPER, DELETE, global, caelestia:clearNotifs
bind = SUPER+ALT, S, global, caelestia:showall
# bind = $kbLock, global, caelestia:lock

# Restore lock
# bindl = $kbRestoreLock, exec, caelestia shell -d
# VER NOTIFACIONES + LOCK - {notis}
bindl = SUPER, ESCAPE, global, caelestia:lock

# Media
bindl = Ctrl+Super, Space, global, caelestia:mediaToggle
bindl = , XF86AudioPlay, global, caelestia:mediaToggle
bindl = , XF86AudioPause, global, caelestia:mediaToggle
bindl = Ctrl+Super, Equal, global, caelestia:mediaNext
bindl = , XF86AudioNext, global, caelestia:mediaNext
bindl = Ctrl+Super, Minus, global, caelestia:mediaPrev
bindl = , XF86AudioPrev, global, caelestia:mediaPrev
bindl = , XF86AudioStop, global, caelestia:mediaStop

# Kill/restart
# bindr = Ctrl+Super+Shift, R, exec, qs -c caelestia kill
# bindr = Ctrl+Super+Alt, R, exec, qs -c caelestia kill; caelestia shell -d
#
# Go to workspace -1/+1
# Toggle special workspace
# bind = $kbToggleSpecialWs, exec, caelestia toggle specialws

# Special workspace toggles
bind = SUPER+ALT, B, exec, caelestia toggle sysmon
# bind = $kbMusic, exec, caelestia toggle music
# bind = SUPER, M, exec, caelestia toggle music
bind = SUPER, M, exec, hyprctl dispatch togglespecialworkspace music # esto hace lo mismo que CAELESTIA
bind = SUPER SHIFT, M, movetoworkspace, special:music
# bind = SUPER SHIFT, M, movetoworkspace caelestia toggle music
# bind = $kbCommunication, exec, caelestia toggle communication
# bind = $kbTodo, exec, caelestia toggle todo

# Apps
# bind = $kbTerminal, exec, app2unit -- $terminal
# bind = $kbBrowser, exec, app2unit -- $browser
# bind = $kbEditor, exec, app2unit -- $editor
# bind = Super, G, exec, app2unit -- github-desktop
# bind = $kbFileExplorer, exec, app2unit -- $fileExplorer
# bind = Super+Alt, E, exec, app2unit -- nemo
# bind = Ctrl+Alt, Escape, exec, app2unit -- qps

# Utilities
bindl = CTRL, Print, exec, caelestia screenshot  # Full screen capture > clipboard
bind = Shift, Print, global, caelestia:screenshotFreeze  # Capture region (freeze)
# bind = Super+Shift+Alt, S, global, caelestia:screenshot  # Capture region
bind = Super+Alt+Ctrl, R, exec, caelestia record -s  # Record screen with sound
# bind = Ctrl+Alt, R, exec, caelestia record  # Record screen
bind = Super+Shift+Alt, R, exec, caelestia record -r  # Record region

# Clipboard and emoji picker
bind = Super SHIFT, Period, exec, pkill fuzzel || caelestia emoji -p


# --- NIVELES DE ZOOM OUT (Disminuir Escala/Alejar) ---

# Nivel 1 (Escala 0.9) - Menor que el por defecto (Zoom Out)
bind = SUPER, minus, exec, hyprctl keyword monitor eDP-1,preferred,auto,1 # Reiniciar Zoom (Escala 1.0)

# Abrir Menú Interactivo de Zoom con SUPER + Igual/Más
bind = SUPER, equal, exec, ~/scripts/zoom_menu.sh

# cerrar terminal divida, kitty,ghostty:
# Control + ;  o Control + ñ

# ═══════════════════════════════════════════════════════════
# [SCRIPT]: KEYBINDS PARA AUTOCLICKER INTERACTIVO EN HYPRLAND
# ═══════════════════════════════════════════════════════════
# ═══════════════════════════════════════════════════════════
# 🖱️  AUTOCLICKER + AUTO-PRESS - KEYBINDS DEFINITIVOS
# ═══════════════════════════════════════════════════════════
# INSTALACIÓN:
# 1. chmod +x ~/wrapper/autoclicker-menu ~/wrapper/autopress-menu
# 2. Agrega estos binds a ~/.config/hypr/hyprland.conf
# 3. hyprctl reload

# ═══════════════════════════════════════════════════════════
# AUTOCLICKER
# ═══════════════════════════════════════════════════════════
bind = , F4, exec, kitty --class=floating -e ~/wrapper/autoclicker-menu
# bind = , F4, exec, kitty --class=floating -e ~/wrapper/autoclicker-menu
# bind = , F4, exec, kitty --class=floating -e ~/.local/bin/autoclicker
bind = , F8, exec, ~/.local/bin/autoclicker stop

# ═══════════════════════════════════════════════════════════
# AUTO-PRESS
# ═══════════════════════════════════════════════════════════
bind = , F6, exec, kitty --class=floating -e ~/wrapper/autopress-menu
bind = , F9, exec, ~/.local/bin/autopress stop
