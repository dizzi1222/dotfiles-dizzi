#!/bin/bash

# --- 1. INSTALACIÓN DE TEMA GTK (Colloid) ---
mkdir -p ~/.themes
if [[ ! -d ~/.themes/Colloid-gtk-theme ]]; then
  git clone --depth 1 https://github.com/vinceliuice/Colloid-gtk-theme.git ~/.themes/Colloid-gtk-theme
fi

cd ~/.themes/Colloid-gtk-theme || { echo "Error: no se pudo acceder a Colloid-gtk-theme"; exit 1; }

# Ejecutar el script de instalación (Modo Dark, Estándar)
./install.sh -t all -c dark -s standard

# --- 2. ESTABLECER APLICACIONES PREDETERMINADAS ---

# TERMINAL: (Funciona en entornos GNOME/Cinnamon/MATE basados en GSettings)
# Esto establece 'kitty' como la aplicación de terminal que se abrirá, por ejemplo,
# cuando otra aplicación pida lanzar una terminal.
gsettings set org.cinnamon.desktop.default-applications.terminal exec 'kitty'

# NEMO EXPLORER: (Utiliza el estándar XDG, que funciona en cualquier entorno Linux)
# Esto establece 'nemo.desktop' como la aplicación predeterminada para abrir directorios
# (inode/directory) y búsquedas guardadas.
xdg-mime default nemo.desktop inode/directory application/x-gnome-saved-search

# ICONOS DE ESCRITORIO [NEMO] (Asegura que Nemo dibuje los iconos en el fondo)
# 1. Desactiva la gestión del escritorio de GNOME (usado por Nautilus, si está instalado).
gsettings set org.gnome.desktop.background show-desktop-icons false
# 2. Activa la gestión del escritorio de Nemo.
gsettings set org.nemo.desktop show-desktop-icons true

# --- 3. CONFIRMACIÓN ---
echo "Tema Colloid instalado correctamente"
notify-send "Nemo y Kitty configurados como Default App & Tema Colloid instalado correctamente"
