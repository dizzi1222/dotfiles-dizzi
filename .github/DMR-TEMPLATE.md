## Developer Merge Report (DMR)

### Pull Request

- **PR:** #`XX` — `<branch>` → `<target>`
- **Merge:** `<type>` por `<author>`
- **Commits:** `<summary>`
- **Notas:** `N/A` si todo está en orden / `<observaciones>`

---

### Cambios estructurales

> Si **no** hubo cambios estructurales, escribe `N/A` en la tabla.

| Archivo | Cambio | Razón |
|---------|--------|-------|
| `N/A` | `N/A` | `N/A` |
| *(o)* `path/to/file.ts` | `old` → `new` | `reason` |

---

### Refactor (si aplica)

> Si **no** hubo refactor, escribe `N/A` en la tabla.

| Archivo | Cambio | Razón |
|---------|--------|-------|
| `N/A` | `N/A` | `N/A` |
| *(o)* `path/to/file.ts` | `old` → `new` | `reason` |

---

### Arquitectura final (si aplica)

> Si **no** cambió la arquitectura, escribe `Sin cambios` o `N/A`.

```
Sin cambios — la arquitectura se mantiene igual.
```
*(o)*
```
diagram
```

---

### Code Review — Resolución

> Si **no** hubo comentarios, escribe `Sin comentarios` en la tabla.

| Severidad | Comentario | Resolución |
|-----------|-----------|------------|
| `Sin comentarios` | `—` | `—` |
| *(o)* 🔴 Critical | `issue` | `fix` |
| *(o)* 🟠 High | `issue` | `fix` |
| *(o)* 🟡 Medium | `issue` | `fix` |

---

### Post-merge State

- [ ] **Check 1** — `N/A` si no aplica / `<descripción>`
- [ ] **Check 2** — `N/A` si no aplica / `<descripción>`
- [ ] *(Agregar más según necesite)*

---

### Notas generales

> Espacio para observaciones adicionales, dependencias pendientes, o confirmar que todo está en orden.

```
N/A — todo en orden, merge validado sin incidencias.
```
